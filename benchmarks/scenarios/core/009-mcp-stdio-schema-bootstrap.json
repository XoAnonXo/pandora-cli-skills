{
  "schemaVersion": "1.0.0",
  "id": "mcp-stdio-schema-bootstrap",
  "title": "Stdio MCP schema bootstrap",
  "description": "Local MCP exposes the same schema bootstrap contract needed by autonomous clients.",
  "transport": "mcp-stdio",
  "assertionId": "schema-bootstrap",
  "parityGroup": "schema-bootstrap",
  "parityExpectedTransports": ["cli-json", "mcp-stdio", "mcp-http"],
  "targetLatencyMs": 5000,
  "weight": 10,
  "dimensions": ["bootstrap", "schema", "mcp", "local-parity"],
  "request": {
    "tool": "schema",
    "arguments": {}
  }
}
